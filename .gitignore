# --- Agent / tool scratch (not part of the project) ---
.claude
GEMINI.md
.codex-remote-attachments/

# NOTE: .ai-sync/ is NOT ignored — it's the real cross-agent context log
# (Claude/Codex/Antigravity), meant to be committed and shared.

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# --- Blender auto-backup files ---
*.blend1
*.blend2

# --- OS junk ---
.DS_Store
Thumbs.db
desktop.ini

# --- Editor junk ---
*.swp
*.swo
*~

# --- Secrets: real credentials, never committed (see *.example.h) ---
secrets.h
**/secrets.h

# --- Agent scratch (Codex nav-cap review artifacts) ---
.tmp_nav_review/

# Blender frame output — regenerable via renders/braillix_anim.py
renders/out/
