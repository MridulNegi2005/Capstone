# Braillix CAD Forensic Audit v6 — 2026-06-04

> **Single source of truth for the pre-patent CAD audit.** Future agents (Claude / Codex /
> Antigravity): READ THIS before touching any `cad/scad/*.scad`. It records what was verified,
> what was a false alarm (do NOT re-"fix"), and the decisions Mridul locked.

---

## 0. How this was produced
3 parallel Explore agents read every core/enclosure/pod SCAD file. Then **every contradictory
claim was re-verified against the actual source** by reading the exact lines. Only verified
facts are below.

---

## 1. ⚠️ FALSE ALARMS — DO NOT "FIX" THESE
Two agents raised "critical bugs" that are **wrong**. Verified against source:

1. **Magnet polarity is CORRECT (not a repulsion bug).**
   - Chain always docks a `+X` face to a `−X` face: `pod+X → cell−X`, `cell+X → cell−X`, …
   - `−X` faces are **N/S/N**, `+X` faces are **S/N/S** → opposite poles meet → **ATTRACT**.
   - Verified: `outer_box.scad:14,130`, `esp32_pod_shell.scad:49`, `esp32_pod_params.scad:63`.
   - The agent error assumed `+X` meets `+X`. It never does in a daisy-chain.
   - **NOTE:** OpenSCAD models magnets as identical cylindrical pockets anyway — polarity is an
     assembly-time decision documented in comments, not geometry. Nothing to change.

2. **`linkage.scad` reference table is only a STALE COMMENT, not broken geometry.**
   - The comment table near the bottom lists `arm_span`/`asm_ang` computed with the OLD
     `inner_radius=8`. The **live code computes these from functions at render time** with the
     current `inner_radius=12.0` (`linkage.scad:55`). Geometry is correct. (Optional: refresh
     the comment for tidiness; not functional.)

---

## 2. 🔴 REAL FINDINGS (verified)
| # | File / line | Issue | Severity | Disposition |
|---|---|---|---|---|
| A1 | `linkage_comb.scad:28` | `inner_radius=8.0` vs `12.0` elsewhere → slits miss feet ~4mm | Latent-critical | Comb was **scrapped** from print pipeline → **DELETE the file** (decision). |
| A2 | `top_plate.scad:23`, `linkage.scad:58` | `col_spacing=4.8mm` vs standard braille ~2.5mm | Product-level | **CLOSED by decision** — keep jumbo (see §3). |
| A3 | `base_plate.scad` hall pocket | magnet r=17.35 vs pocket y=20 → real gap **2.65mm**; comment says 1.8mm | Medium | Fix the comment; gap validated OK on breadboard (hall saturates within a few mm). Move pocket inward only if a bench test later fails. |
| A4 | `outer_box.scad` / `esp32_pod_shell.scad` side walls | pogo windows + magnet pockets are horizontal holes → FDM sag | Medium (PETG) | **Add printed-in bridges/chamfers** (these parts are PETG). |
| A5 | `outer_box.scad` floor gutters | 4mm gutters too narrow for real Dupont/JST bundles; cable hooks undersized | Medium | Widen gutters to 6–8mm; enlarge hooks. |
| A6 | `top_plate.scad:27` | hole 2.5mm vs nub 2.2mm = 0.15mm/side | Low | **No change** — top_plate is RESIN, tolerance fine. |
| A7 | `mid_plate.scad` collar | collar ID 29.5 on 29mm motor body = 0.25mm/side | Low | Measure real motor body before final print. |

---

## 3. ✅ LOCKED DECISIONS (Mridul, 2026-06-04)
- **Braille pitch: KEEP 4.8mm "jumbo" (Option B).** No top_plate/linkage pitch redesign. Framed
  as **large-format / braille-learner** device. Spring pockets unchanged.
- **Material split:**
  - **RESIN/SLA:** `braille_cam`, `top_plate`, `nav_cap` ×3 (precision/tactile).
  - **PETG/FDM:** `outer_box`, `mid_plate`, `base_plate`, `esp32_pod_shell`, `esp32_pod_lid`,
    `pogo_end_cap` (TPU).
- **Do NOT make FDM-tolerance edits to resin parts.** (No cam-gap widening, no top-plate hole
  reaming, no FDM cam variant.) Resin handles 0.1mm gaps + 2.5mm holes fine.
- **Bridges only where PETG needs them** (A4). That's the sole manufacturability edit.
- **Patent doc DEFERRED** to the very end of the polish pass — keep raw novelty notes only (§5).

---

## 4. BRAILLE PITCH — NUMBERS (kept as rationale for the eventual patent doc)
- **Standard braille:** dot Ø 1.44–1.6mm, dot height 0.46–0.50mm, within-cell pitch ~2.5mm,
  cell-to-cell ~6.1–6.5mm, line-to-line ~10mm.
- **Braillix:** horizontal `col_spacing=4.8mm` (~1.9× wide), vertical `row_spacing=2.6mm` (≈ std),
  dot = 2mm ball, nub 2.2mm, hole 2.5mm, rise 0.8mm.
- **Why 4.8:** two 2.5mm holes at 2.5mm pitch touch edges and merge; the 2mm ball/2.2mm nub is
  too fat to pack tighter. A standard build would need ball→1.5, nub→1.6, hole→1.9, pitch→2.5,
  and the 4.5mm return-spring pockets would overlap (need micro-springs OR gravity+shared return).
- **DEEPEST TRUTH (architecture):** each cell is a **68mm cube → characters sit 68mm apart** vs
  6.1mm standard. A 28BYJ-48 is **28mm Ø**, so one-motor-per-character at reading pitch is
  physically impossible. → Braillix is honestly a **modular braille teaching / single-character
  display**, not a compact multi-char reader. Frame it that way in the patent.

---

## 5. NOVELTY NOTES (for the deferred patent doc)
Defensible combination: single-stepper **indexed 6-track binary cam disc** (64-state) driving 6
**ball-tipped linkages** to raise braille dots + **hall edge-midpoint homing** + **magnetic+pogo
daisy-chain modularity** + **anti-reversal dock keying**. Prior art to distinguish: piezo
bimorph displays, Orbit Reader. Large-format pitch is a deliberate accessibility/teaching choice.

---

## 6. QOL / NEW FEATURES BEING ADDED THIS PASS
1. ~~**Anti-reversal dock keying** (post/slot)~~ — **REVERTED 2026-06-04 per Mridul** (external
   bump looked bad on the clean brick). Analysis for the record: magnets DO catch a wrong-*face*
   dock (repel), but do NOT catch an **upside-down** dock (magnet pattern is symmetric about the
   X flip → still attracts). Upside-down reverses the 4 pogo pins' Y-order → upstream 5V lands on
   the flipped cell's SCL pin → **not a supply short, but 5V on the I²C lines can damage the
   non-5V-tolerant ESP32 GPIOs.** Mitigation deferred to the CONNECTOR/electrical level (cleaner
   than a bump): options = (a) use a polarized/keyed 4-pin pogo connector, (b) place the pogo
   window enough off-vertical-center that a flipped cell's pins simply don't meet (open circuit =
   safe), or (c) series protection on the bus. TODO when the real pogo part is chosen.
2. **Pod-vs-cell tactile ID** — raised braille ⠿ (all-6) on pod lid (also clears the long-pending
   "⠿ on pod" task).
3. **Nav-cap braille labels** P/S/N under the ◁○▷ domes.
4. **Power-jack guard ring** around the barrel jack.
5. Optional: end-cap orientation bump; 1/2/3 pips for cell position in chain.

---

## 7. STATUS — IMPLEMENTED (2026-06-04)
Plan v6.0 fully implemented this session. Resin geometry frozen; all edits on PETG parts.
Done: deleted `linkage_comb`; anti-reversal dock keying (cell +X post / −X slot + pod +X post);
PETG sacrificial bridges (pogo windows + USB); wire gutters 4→7mm; pod ⠿ ID marker + jack guard
ring (lid); P/S/N braille on pod front wall; end-of-chain ridge on end-cap; hall-gap comment
corrected. All changed STLs render `Simple: yes`; print_batch + zip rebuilt.
Deferred: patent claims doc (end of polish); optional cell-position pips; relocate hall to
y≈17.35 only if homing proves weak on the real assembly.
